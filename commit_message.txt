fix(ci): Corregir errores de linting y cambiar a Trivy en Docker

Se solucionan dos problemas persistentes en el pipeline de CI:

1.  **Error de Linting (E402/F811):** Se elimina una importación duplicada de `timedelta` en `config/settings.py` y una importación mal ubicada de `ScopedRateThrottle` en `crud_app/tests.py`. Esto resuelve definitivamente los fallos del job `lint`.

2.  **Error en Security Scan:** Se reemplaza el uso de la acción `aquasec/trivy-action`, que fallaba al no poder ser resuelta, por una ejecución directa de Trivy dentro de un contenedor Docker. Este método es más robusto y soluciona el fallo del job `security_scan`.